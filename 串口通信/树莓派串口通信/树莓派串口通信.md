# 树莓派串口通信

> 问题发现：在树莓派4b中安装ubuntu24.04 LTS后，在终端使用`ls -l /dev/serial* `命令，发现压根没有`/dev/serial0`和`/dev/serial1`，也没有`ttyS0`和`ttyAMA0`。之前在22.04里只会遇到没有`serial0`或没有`serial1`的情况，并且网上能找到很多解决方案文档。
>
> 没有serial0和ttyAMA0的话，就无法使用树莓派自带串口了。。并且由于24.04实在是太新了，无论内网外网都根本找不到相关问答帖子或者文档。。